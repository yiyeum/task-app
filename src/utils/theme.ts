import { createMuiTheme } from "@material-ui/core"

export const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#7FF0B0'
        },
        secondary: {
            main: '#FFEC87'
        },
        text: {
            primary: '#373737',
            secondary: '#757575'
        }
    }
});